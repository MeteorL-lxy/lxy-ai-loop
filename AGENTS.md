# Project Context

This project uses a project-specific Barry Video / AI Loop context file.

When working in this repository, first read:

`/Users/xinyuliu/.codex/memories/barry-video-loop-context.md`

Use that file only as context for this project. Do not apply it to unrelated repositories or conversations.

Important:

- Do not expose secrets, passwords, app secrets, API keys, or tokens in user-facing reports.
- Prefer the installed Barry Video workflow and project scripts over ad-hoc one-off commands.
- Keep short-drama, novel, report, Feishu push, and cleanup logic aligned with the project memory.
